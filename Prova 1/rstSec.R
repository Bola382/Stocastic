
rstSec = function(n,mu,sigma,lambda,nu,xvec){
 y = NULL
 h = function(x) log(dst(x,mu,sigma,lambda,nu)) # log densidade objetivo
 un = function(x){ # funcao envelope
  
  }
 
 yvec = h(x)
 
 while(length(y)<n){
  
 }
}